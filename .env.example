# ZeroPass — Confidential Credentials on Midnight Network Environment Variables Example
# Copy this file to .env and configure for your environment.

# ─── Active Network ─────────────────────────────────────────────────────────
# Specify the network to connect to: 'undeployed', 'preview', or 'preprod'.
NETWORK=preprod
VITE_NETWORK=preprod

# ─── Contract Address ───────────────────────────────────────────────────────
# Midnight Smart Contract Address (Hex, 64 chars). Set after `npm run deploy`.
CONTRACT_ADDRESS=
VITE_CONTRACT_ADDRESS=

# ─── Frontend API Endpoint ──────────────────────────────────────────────────
# Active API endpoint URL for the frontend in production (e.g. deployed backend or tunnel).
VITE_API_BASE_URL=https://YOUR-ACTIVE-API-ENDPOINT/api

# ─── Network overrides (optional) ───────────────────────────────────────────
# Override the default URLs for the active network. Useful when running a local
# devnet or a custom node. The PolkadotNodeClient (wallet SDK) uses MIDNIGHT_NODE_URL
# for its WebSocket relay connection.
# MIDNIGHT_NODE_URL=ws://127.0.0.1:9944
# MIDNIGHT_INDEXER_URL=http://127.0.0.1:8088/api/v4/graphql
# MIDNIGHT_INDEXER_WS_URL=ws://127.0.0.1:8088/api/v4/graphql/ws
# MIDNIGHT_PROOF_SERVER_URL=http://127.0.0.1:6300

# ─── API Server ─────────────────────────────────────────────────────────────
# PORT=8080
# API_URL=http://127.0.0.1:8080